{
  "name": "wa-gateway-adaptor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run index-compat.ts",
    "start:improved": "bun run index-improved.ts",
    "start:original": "bun run index.ts",
    "dev": "bun --hot run index-compat.ts",
    "dev:improved": "bun --hot run index-improved.ts",
    "dev:original": "bun --hot run index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.25.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
